# Wrangler configuration for Cloudflare Pages
# Used by `wrangler pages deploy` (run from apps/reader after `pnpm build:cloudflare`)

name = "flow-reader"
compatibility_date = "2024-01-01"
pages_build_output_dir = "out"
